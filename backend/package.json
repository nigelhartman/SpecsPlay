{
  "name": "lyria-vision-proxy",
  "version": "2.0.0",
  "description": "HTTP server: receives camera frame + genre from Spectacles, calls Lyria REST API, returns MP3 URL",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=../.env server.js",
    "dev": "node --env-file=../.env --watch server.js"
  },
  "dependencies": {
    "@google/genai": "^1.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
